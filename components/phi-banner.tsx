export function PhiBanner() {
  return (
    <div className="border-b border-warn/30 bg-[#f6eadf] px-4 py-2 text-center text-xs leading-relaxed text-ink">
      Production PHI may be stored here only after BAAs and infrastructure controls are approved
      by Von & Bick Healthcare Associates. Seed accounts use <strong>SYNTHETIC</strong> Jane/John
      Demo records. Zoom Healthcare is video + BAA only — the chart lives in this app.
    </div>
  );
}
